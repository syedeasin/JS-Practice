var myObj = {
  name: "Syed Easin",
  age: 25,
  occupation: "Software Engineer",
  gender: "Male",
};

var keys = Object.keys(myObj); //Object.keys(arrayName) use kore key er array banano jay
var values = Object.values(myObj); //Object.values(arrayName) use kore value er array banano jay
var entries = Object.entries(myObj); //Object.entries(arrayName) use kore total entry er array banano jay

console.log(keys);
console.log(values);
console.log(entries);
